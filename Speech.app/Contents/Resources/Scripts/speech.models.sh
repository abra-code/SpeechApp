# speech.models - opens the Models window (Models > Manage Models...). The
# window's work is done by speech.models.init and the Models window's poller: a non-blocking
# window's own command script runs at an unpredictable time, so it does nothing.

exit 0
